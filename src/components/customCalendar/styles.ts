import { StyleSheet } from "react-native";

export const customCalendarStyles = StyleSheet.create({
    calendarContainer: {
        height: 200,
        paddingTop: 20,
        paddingBottom: 10,
    }
});